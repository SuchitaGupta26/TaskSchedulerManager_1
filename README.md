# TaskSchedulerManager_1
A command-line application where users add tasks with different priority levels (High, Medium, Low). The program automatically sorts and presents the most urgent tasks first.
